// Re-exports the SubtaskForm component as default
// Allows importing from the folder directly (e.g., import SubtaskForm from "../SubtaskForm")
export { default } from "./subtaskForm";

// Re-exports the SubtaskFormProps type
// Enables external files to access the component's prop types
export type { SubtaskFormProps } from "./subtaskForm.types";
